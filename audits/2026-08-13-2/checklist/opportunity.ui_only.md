# OPPORTUNITY — UI-only values (panel shows, export does not carry)

These are rendering/derivation-layer values; none of them affect the wire.

1. filter pills (Active only / Confirmed+ / Hide gates / Hide overlays)
2. header tone colors (bull/warn/neutral accent)
3. lifecycle state labels (Warming (n/m) — from indicator_lifecycle)
4. pipeline status badge (LIVE / LIVE)
5. price formatting ($, %, 0b age suffix)
6. refresh counter (last update pulse)
